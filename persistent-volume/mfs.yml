kind: PersistentVolume
apiVersion: v1
metadata:
  name: mfs-volume
  labels:
    type: local
spec:
  capacity:
    storage: 8Gi
  accessModes:
    - ReadWriteMany
  flexVolume:
    driver: "lizardfs"
    options:
      host: "localhost"
      port: "30421" 
  persistentVolumeReclaimPolicy: Recycle
